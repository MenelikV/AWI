<div class="jumbotron jumbostyle">
    <div class="container-fluid">
        <!-- Title and Activity container-->
        <div class="row">
            <div class="col md-4"></div>
            <div class="col md-4">
                <h1 class="display 4 text-center" style="padding-top:2rem;"> Flight Overview </h1>
            </div>
            <div class="col md-4">
                <div class="row">
                    <div class="col-md-8">
                        <div class="form-group " style="padding-top:2rem;">
                            <div class="input-group mb-3">
                                <div class="input-group-prepend">
                                    <label class="input-group-text" for="activity">Activity</label>
                                </div>
                                <select class="custom-select" id="activity" v-model.trim="formData.activity">
                                    <option value="1">Autoval</option>
                                    <option value="2">Activity</option>
                                </select>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <hr style="padding-bottom:2rem;">
        <!-- End title and Activity container-->

    <div class="row">
        <div class="col md-6">
            <!-- Flight Summary container -->
            <div class="container-fluid" style="background-color:white; overflow:auto;">
                <h4 style="padding-top:0.5rem">
                    Flight Summary
                </h4>
                <table class="table table-striped text-center width=100%">
                </table>
            </div>
            <!-- End Flight Summary container -->
        </div>

        <div class="col md-6">
            <!-- PVOL container -->
            <div class="container-fluid" style="background-color:white; overflow:auto;">
                <h4 style="padding-top:0.5rem">
                    <%= (name) %>
                </h4>

                <table id="pvol" class="table table-striped table-hover text-left width=100%">

                    <thead>
                        <tr>
                            <% for(var i=0; i< headers.length; i++) { %>
                            <%if(headers[i] == "START" ||  headers[i] == "END") {%>
                            <th>
                                <%= headers[i] %>
                            </th>
                            <% } 
                            } %>
                        </tr>
                    </thead>

                    <tbody>  
                        <!-- Iterate over errors rows-->
                        <% for(var j=0; j< data.length; j++) { %>
                        <tr style="cursor : pointer;">
                            <!-- Iterate over each field data-->
                            <% for(var k=0; k< headers.length; k++) { %>
                            <%if(headers[k] == "START") {%>
                            <td>
                                GMT
                                <%=  data[j][headers[k]] %>
                            </td>
                            <% }else if ( headers[k] == "END") { %>
                            <td>
                                GMT
                                <%=  data[j][headers[k]] %>
                                <br>
                                <p>ERROR: <%= CSVerrors[j].length %></p>
                            </td>
                            <% } } %>
                        </tr>
                        <% } %>
                    </tbody>

                </table>
                <hr>
            </div>
            <!-- End PVOL container -->
        </div>
        <br>
    </div>
</div>
</div>